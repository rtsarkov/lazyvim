return {
  "nvim-telescope/telescope.nvim",
  opts = {
    defaults = {
      hidden = true,       -- показывать скрытые файлы (.git, .env)
      no_ignore = true,    -- игнорировать .gitignore, показывать все файлы
    },
  },
}

